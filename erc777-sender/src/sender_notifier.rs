use casper_types::bytesrepr::Bytes;
use casper_types::U256;
use Address;

pub fn tokens_to_send(
    operator: Address,
    from: Address,
    to: Address,
    amount: U256,
    user_data: Bytes,
    operator_data: Bytes
) {

}